## 1. Verify live Groq model catalog (VERIFY — do first, before coding)

- [x] 1.1 **VERIFY**: Call `GET https://api.groq.com/openai/v1/models` with a real `GROQ_API_KEY` (or check the Groq dashboard model list) and confirm `openai/gpt-oss-120b`, `openai/gpt-oss-20b`, and `qwen/qwen3.6-27b` are still active/live model ids. This design's catalog table was fetched from Groq's public docs on 2026-08-11 — reconfirm at implementation time since Groq's Preview-tier models (including `qwen/qwen3.6-27b`) can be pulled at short notice. **Result (2026-08-11 re-verified at implementation time): all 3 confirmed live via `GET /openai/v1/models`.**
- [x] 1.2 **VERIFY**: Confirm `llama-3.3-70b-versatile`'s documented 2026-08-16 shutdown date is still accurate (check `console.groq.com/docs/deprecations`) — if it has already been removed from the catalog by implementation time, ensure no code path still references it as a live fallback. **Result: confirmed accurate — `console.groq.com/docs/deprecations` still lists 08/16/26 shutdown for both `llama-3.3-70b-versatile` and `llama-3.1-8b-instant`, recommending `openai/gpt-oss-120b`/`qwen/qwen3.6-27b` and `openai/gpt-oss-20b` respectively — exact match with design.md.**
- [x] 1.3 If any candidate model id has changed or been removed since this design was written, update `design.md`'s catalog table and the allow-list in task 2.1 accordingly before proceeding. **No changes needed — catalog matches design.md exactly.**

## 2. Define the model allow-list and rotation toggle

- [x] 2.1 In `backend/app/llm/groq_provider.py`, replace/augment `DEFAULT_GROQ_MODEL` with an `ALLOWED_GROQ_MODELS` constant (list) containing `["openai/gpt-oss-120b", "openai/gpt-oss-20b", "qwen/qwen3.6-27b"]` (per design.md Decision 1), with an inline comment documenting why `llama-3.3-70b-versatile` / `llama-3.1-8b-instant` / `qwen/qwen3-32b` and the excluded categories (moderation, agentic, safety) are not included.
- [x] 2.2 Add an `is_rotation_enabled()` (or equivalent) helper: `True` when `GROQ_MODEL` env var is unset/empty, `False` otherwise — reusing the existing empty-string-falls-back-to-default handling already covered by `test_empty_env_falls_back_to_default`.

## 3. Implement model selection and in-provider retry

- [x] 3.1 Add a model-selection function (e.g. `select_groq_models(n: int = 2) -> list[str]`) that returns `n` distinct models via `random.sample(ALLOWED_GROQ_MODELS, n)` when rotation is enabled, or `[get_groq_model()]` (single pinned model) when `GROQ_MODEL` is set.
- [x] 3.2 Add `call_groq_with_rotation(messages)` in `groq_provider.py`: calls `call_groq(messages, model=...)` (extend `call_groq` to accept an optional `model` override parameter, defaulting to `get_groq_model()`) against the first selected model; on `GroqRateLimitError`/`GroqServerError`/`GroqTimeoutError`, retries once against the second selected model (only when rotation is enabled — no retry when `GROQ_MODEL` pins a single model, per design.md Decision 3); re-raises the second attempt's error if it also fails.
- [x] 3.3 Update `backend/app/llm/suggestions.py` to call `call_groq_with_rotation()` instead of `call_groq()` directly, keeping the existing Groq → Cloudflare fallback structure and exception handling unchanged (the wrapper raises the same `GroqError` subtypes the existing `except` clauses already handle).

## 4. Tests

- [x] 4.1 Extend `backend/tests/test_groq_provider.py`: rotation-enabled requests only ever select from `ALLOWED_GROQ_MODELS`; `GROQ_MODEL` set disables rotation and pins to that exact model (extend existing `TestGetGroqModel`/`TestCallGroqUsesConfiguredModel` coverage rather than duplicating it).
- [x] 4.2 Add tests for `call_groq_with_rotation`: first-model success (no retry attempted), first-model retriable failure + second-model success (returns second model's result, confirms 2 distinct models were used), both models fail retriably (raises, confirming the caller's fallback-to-Cloudflare path is reached), `GROQ_MODEL` set + retriable failure (no retry, error propagates immediately — matches pre-change behavior).
- [x] 4.3 Add/extend a `suggestions.py`-level test confirming the full chain still falls back to Cloudflare only after both attempted Groq models fail (update or extend existing failover tests in `backend/tests/test_proposals.py` or a dedicated suggestions test file, whichever currently holds this coverage — confirm which file it's in before adding a duplicate). **Found coverage lives in `backend/tests/test_llm_suggestions.py`; updated its mocks to `call_groq_with_rotation` and added `test_groq_rotation_retries_both_models_before_cloudflare_fallback`.**
- [x] 4.4 Run the full backend test suite (`pytest backend/tests/`) and confirm no regressions in existing Groq/Cloudflare/parser tests. **Result: 95 passed, 2 skipped (pre-existing, unrelated), 0 failures.**

## 5. Documentation

- [x] 5.1 Update `AGENTS.md` "AI Suggestion Generation" section: replace the single-model description with the rotation pool (list the 3 allow-listed models), the `GROQ_MODEL` override semantics ("disables rotation, pins to one model"), and the in-provider retry-then-fallback behavior. Update the "Default" row's latency/model description in the existing provider table.
- [x] 5.2 Check `docs/SYSTEM-DESIGN.md` for any content duplicating the single-model Groq description (mirrors `AGENTS.md`'s architecture section per its stated update convention) and update it to match if present. **Checked — `docs/SYSTEM-DESIGN.md` only mentions "Groq → Cloudflare failover" generically, no specific model id, so no update needed.**
- [x] 5.3 Add a brief note (e.g. in `AGENTS.md` or a code comment near `ALLOWED_GROQ_MODELS`) that the allow-list must be manually reviewed if Groq announces further deprecations, since there is no runtime catalog-refresh mechanism (per design.md Non-Goals). **Added both as an AGENTS.md "Maintenance note" bullet and as a code comment above `ALLOWED_GROQ_MODELS` in `groq_provider.py`.**

## 6. Manual live verification

- [x] 6.1 With `GROQ_API_KEY` configured and `GROQ_MODEL` unset, manually trigger `POST /api/suggestions` (or the equivalent frontend flow) enough times to observe at least one request landing on each of the 3 pool models (check Groq dashboard per-model usage or add temporary debug logging of the selected model), confirming each produces a coherent, correctly-parsed Japanese suggestion for the same sample input. **Done via a direct script calling `call_groq(messages, model=...)` for all 3 pool models with the real prompt/parser. Initial run surfaced a real bug (see design.md Decision 5 addendum): `qwen/qwen3.6-27b`'s default reasoning mode emitted a `<think>` block that exhausted `max_tokens` before any JSON appeared, causing the parser to silently return the system prompt's placeholder text. Fixed by adding `reasoning_effort: "none"` for this model (`QWEN_REASONING_MODELS` in `groq_provider.py`) — re-verified all 3 models produce correct, coherent, correctly-parsed Japanese suggestions after the fix.**
- [x] 6.2 Manually verify the `GROQ_MODEL` override still works end-to-end (set it to one specific pool model, confirm only that model is used and no rotation/retry occurs). **Verified: `GROQ_MODEL=openai/gpt-oss-120b` → `is_rotation_enabled()` returns `False`, `call_groq_with_rotation()` used exactly that model, correct output returned.**
- [x] 6.3 If feasible, manually simulate a single-model failure (e.g. temporarily set `ALLOWED_GROQ_MODELS` to include an invalid id first) to confirm the in-provider retry against the second model actually recovers the request instead of failing over to Cloudflare — revert the temporary change afterward. **Live-tested with a mocked `select_groq_models` returning an invalid model id first: it correctly returned a non-retriable 404 `GroqError` with no retry attempted (matches design — only 429/5xx/timeout are retriable, and a bad model id isn't transient) rather than silently retrying pointlessly. The retriable-failure-then-recover path (429/5xx/timeout) itself is impractical to force live without burning real API quota/waiting on genuine rate limits, so it is covered by the deterministic automated tests instead (`test_first_model_retriable_failure_second_model_succeeds`, `test_timeout_then_success`, `test_groq_rotation_retries_both_models_before_cloudflare_fallback` in the test suite), which directly assert two distinct models were attempted and the second model's result was returned.**
